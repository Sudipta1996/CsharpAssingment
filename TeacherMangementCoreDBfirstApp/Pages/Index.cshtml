@page
@*@model IndexModel
@{
    ViewData["Title"] = "Home page";
}*@
@*@model IEnumerable<TeacherMangementCoreDBfirstApp.Models.Teacher>


@{
    ViewBag.Title ="Index";
}
<h2>Employeee Details</h2>
<p>
    @Html.ActionLink("Add Teacher", "AddTeacher")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Id)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Name)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Email)
        </th>
    </tr>
    @foreach (var item in Model)
    {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Id)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Name)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Email)
            </td>*@
            
            @*<td>
                @Html.ActionLink("Edit", "EditEmployee", new { EmployeeId = item.EmployeeID }) |
                @Html.ActionLink("Delete", "DeleteEmployee", new { EmployeeId = item.EmployeeID })
            </td>*@
        @*</tr>
    }
</table>*@